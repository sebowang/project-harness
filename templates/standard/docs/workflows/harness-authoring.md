# Harness Authoring

## 使用条件

- 回归已经发生或很可能重复发生。
- 共享行为可以在不启动完整产品的情况下检查。
- 仅靠代码审查无法证明所需行为。

## 步骤

1. 定义一个可观察契约及明确失败信号。
2. 复用项目已经验证的测试或运行工具。
3. fixture 使用合成、确定性数据，并与生产隔离。
4. 全部断言通过时才返回退出码 `0`。
5. 记录命令、前置条件、输入、输出和覆盖范围。
6. 需要统一执行时，把命令加入 `harness.config.json`。

不为满足目录结构而创建空 Harness。
