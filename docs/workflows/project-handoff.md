# Project Handoff

仅在任务必须跨会话或跨实施者继续时，创建或更新一个任务专属交接文件。

交接内容包括：

- 目标和验收条件
- 已完成工作和变更文件
- 实际验证命令与结果
- 未解决问题与风险
- 接下来三个具体步骤
- 精确恢复位置

不同时创建 `session-state`、`progress-map` 和 `command-history` 等多份状态文件。任务完成后按项目文档策略删除或归档交接文件。
